# 갓 설치한 Grafana 는 데이터소스가 비어 있다. 이 파일이 없으면 대시보드가 그려지지 않는다.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://127.0.0.1:9090
    isDefault: true
